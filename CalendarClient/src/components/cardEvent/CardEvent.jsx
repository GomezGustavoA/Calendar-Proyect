import styles from "./cardEvent.module.css"

const CardEvent = () => {
    return(
        <div className={styles.container}>
            {/* <h2>{event.title}</h2>
            <p>Fecha de inicio: {event.startStr}</p>
            <p>Fecha de fin: {event.endStr}</p> */}
        {/* <button onClick={onClose}>Cerrar</button> */}
      </div>
    )
}

export default CardEvent;